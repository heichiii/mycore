RV64架构**共有32个通用整数寄存器**（`x0`-`x31`），以及**32个浮点寄存器**（`f0`-`f31`）。

### 📝 通用整数寄存器 (x0 - x31)

这32个寄存器都是**64位宽**的。每个寄存器都有其特定的ABI（应用程序二进制接口）名称和用途。

下表整理了这些寄存器及其用途:

| 寄存器 | ABI名称 | 用途 | 调用约定 |
| :--- | :--- | :--- | :--- |
| `x0` | `zero` | **硬连线为0**，写入无效 | N/A |
| `x1` | `ra` | 返回地址（Return Address） | Caller（调用者保存） |
| `x2` | `sp` | 栈指针（Stack Pointer） | Callee（被调用者保存） |
| `x3` | `gp` | 全局指针（Global Pointer） | N/A |
| `x4` | `tp` | 线程指针（Thread Pointer） | N/A |
| `x5`-`x7` | `t0`-`t2` | 临时寄存器（Temporaries） | Caller |
| `x8` | `s0`/`fp` | 保存寄存器/帧指针 | Callee |
| `x9` | `s1` | 保存寄存器 | Callee |
| `x10`-`x11` | `a0`-`a1` | 函数参数/返回值 | Caller |
| `x12`-`x17` | `a2`-`a7` | 函数参数 | Caller |
| `x18`-`x27` | `s2`-`s11` | 保存寄存器 | Callee |
| `x28`-`x31` | `t3`-`t6` | 临时寄存器 | Caller |

### 💡 补充说明

*   **关于 `x0`**：`x0` 寄存器是一个特殊的存在，它**硬连线为常数0**，任何写入它的操作都不会生效。
*   **浮点寄存器 (f0 - f31)**：如果处理器实现了 “F” 和 “D” 标准扩展，就会额外增加**32个64位宽的浮点寄存器**。它们也有各自的ABI名称和用途。
*   **向量寄存器**：对于支持 “V” 扩展的处理器，还会有**32个128位宽的向量寄存器**。